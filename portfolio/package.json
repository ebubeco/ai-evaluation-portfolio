{
  "name": "ebube-okeke-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.ts --host 0.0.0.0",
    "build": "vite build --config vite.config.ts",
    "start": "npx serve -s dist/public -l 3000"
  },
  "devDependencies": {
    "vite": "^6.3.0",
    "@types/node": "^22.0.0"
  }
}
